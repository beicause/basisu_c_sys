[package]
name = "test_scene"
version = "0.0.0"
edition = "2024"
publish = false

[lib]
name = "test_scene_lib"
crate-type = ["lib", "cdylib"]

[dependencies]
bevy = { version = "0.18" }
bevy_basisu_loader = { path = "../.." }
